# Docker Deployment for Frontend

## Tasks
- [x] Create Dockerfile in dataanalitycs directory with multi-stage build (Node for building, nginx for serving)
- [x] Create .dockerignore file to exclude unnecessary files
- [x] Create docker-compose.yml for easy deployment (optional)
- [ ] Build the Docker image
- [ ] Run the container to test deployment
- [ ] Verify the app serves correctly
